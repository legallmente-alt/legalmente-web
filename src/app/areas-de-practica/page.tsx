import { notFound } from "next/navigation";

/**
 * LegalMente — The Editorial Instrument.
 * This legacy route is intentionally not part of the public educational
 * surface; canonical entry points live in the relational index and catalog.
 */
export default function AreasDePracticaPage() {
  notFound();
}
